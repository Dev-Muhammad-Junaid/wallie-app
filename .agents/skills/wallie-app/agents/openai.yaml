interface:
  display_name: "Wallie App"
  short_description: "Repo-specific patterns and workflows for wallie-app"
  default_prompt: "Use the wallie-app repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true